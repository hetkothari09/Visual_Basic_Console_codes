Imports System.Console
Module Module1

    Sub Main()
        Dim a As Integer
        Dim b As Integer
        Dim c As Integer
        ' WriteLine("Hello World")
        WriteLine("Enter 3 values : ")
        a = ReadLine()
        b = ReadLine()
        c = ReadLine()

        WriteLine()
        WriteLine("{0} is the value of a", a)
        WriteLine("{0} Is the value Of b", b)
        WriteLine("{0} Is the value Of c", c)

        ReadLine()

    End Sub

End Module
